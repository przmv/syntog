syntog - toggle synaptics touchpad
==================================


Requirements
------------
In order to build syntog you need the Xlib header files.


Installation
------------
Edit config.mk to match your local setup (syntog is installed into
the /usr/local namespace by default).

Afterwards enter the following command to build and install syntog
(if necessary as root):

    make clean install


Running syntog
--------------
Simply invoke the 'syntog' command.
